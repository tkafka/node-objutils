{
  "name": "objutils",
  "description": "Utils for working with objects similarly to arrays, work in progress.",
  "email": "tk+node-objutils@tomaskafka.com",
  "repository": "https://github.com/tkafka/node-objutils.git",
  "version": "1.0.4",
  "private": false,
  "main": "./objutils.js",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
		"format": "pnpm exec biome format --write .",
		"format:check": "pnpm exec biome check --formatter-enabled=true --linter-enabled=false .",
    "lint": "biome lint ."
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^3.0.9",
    "@biomejs/biome": "1.6.3"
  }
}
